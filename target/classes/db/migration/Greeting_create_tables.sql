create table greetings (
                    id INT PRIMARY KEY AUTO_INCREMENT NOT NULL,
                    name varchar(40),
                    greeted varchar(40),
                    counter int
                );